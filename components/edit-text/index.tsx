'use client';

export { default as TextEditor } from './TextEditor';
export type {
  EditorHandle,
  EditorProps,
  WordStats,
  SerializedDocument,
  ExportOptions,
  ExportResult,
  DatabaseTable,
  QuickText,
  Marker,
  PageConfig,
  PageSizeKey,
  Margins,
  StyleState,
  EditorState,
} from './types';
